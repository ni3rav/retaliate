{
  "name": "retaliate",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "esbuild src/app.jsx --bundle --outfile=dist/bundle.js --jsx=transform",
    "server": "node server.js",
    "dev": "concurrently \"npm run server\" \"esbuild src/app.jsx --bundle --outfile=dist/bundle.js --watch --jsx=transform\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "concurrently": "^9.1.2",
    "esbuild": "^0.25.4"
  }
}
