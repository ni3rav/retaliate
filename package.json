{
  "name": "retaliate",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "esbuild src/app.jsx --bundle --outfile=dist/bundle.js --jsx=transform",
    "dev": "esbuild src/app.jsx --bundle --outfile=dist/bundle.js --watch --jsx=transform"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "esbuild": "^0.25.4"
  }
}
